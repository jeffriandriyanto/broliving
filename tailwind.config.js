// tailwind.config.js
export default {
  theme: {
    container: {
      center: true,
      padding: {
        DEFAULT: '2rem',  // padding horizontal default, bisa kamu sesuaikan
        sm: '2rem',
      },
    },
    extend: {
      // ... tema tambahan
    }
  }
}
